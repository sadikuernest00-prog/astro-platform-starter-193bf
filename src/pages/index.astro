---
const featuresBorrower = [
  "Reach real people willing to lend, not just banks.",
  "Borrow in normal money or crypto.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms directly with lenders."
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Lend using bank transfer or crypto.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit."
];
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Amicbridge – Borrow, Lend & Crypto Invest</title>
    <meta
      name="description"
      content="Amicbridge connects people for loans, lending and crypto investment opportunities using wallets, stablecoins and tokenized assets."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        color-scheme: dark;
        --bg: #020617;
        --card: #020617;
        --accent: #22c55e;
        --text: #e5e7eb;
        --muted: #9ca3af;
        --border: #1f2933;
      }
      body {
        margin: 0;
        font-family: system-ui, sans-serif;
        background: radial-gradient(circle at top, #0f172a 0, #020617 55%);
        color: var(--text);
      }
      .shell { max-width: 1100px; margin: 0 auto; padding: 2.5rem 1.5rem 4rem; }
      header { display:flex; justify-content:space-between; align-items:center; margin-bottom:2rem;}
      .logo { display:flex; gap:0.6rem; align-items:center; font-weight:600; letter-spacing:0.04em; }
      .logo-mark { width:26px;height:26px;border-radius:999px;background:radial-gradient(circle at 30% 20%,#4ade80,#16a34a);display:flex;align-items:center;justify-content:center;color:#022c22;font-size:0.9rem;}
      nav a {font-size:0.9rem;color:var(--muted);text-decoration:none;margin-left:1.2rem;}
      nav a.button {color:#022c22;background:var(--accent);padding:0.45rem 0.95rem;border-radius:999px;}

      h2 { margin-bottom:0.6rem; }
      .btn {padding:0.6rem 1.4rem;border-radius:999px;font-size:0.9rem;font-weight:500;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;}
      .btn-primary {background:var(--accent);color:#022c22;}
      .btn-outline {border:1px solid var(--border);color:var(--text);}

      .card { background:rgba(15,23,42,0.8);border-radius:1.2rem;padding:1.5rem;border:1px solid rgba(148,163,184,0.18);}
      .section-grid{display:grid;gap:1.5rem;margin-top:2.8rem;}
      @media(min-width:800px){ .section-grid{grid-template-columns:repeat(2,minmax(0,1fr));} }

      .token-grid {display:grid; gap:1rem; margin-top:1rem;}
      @media(min-width:700px){ .token-grid{grid-template-columns:repeat(4,minmax(0,1fr));} }
      .token-card {border:1px solid rgba(148,163,184,0.25);border-radius:0.9rem;padding:1rem;background:rgba(4,16,34,0.7);}
      .token-card strong{color:var(--accent);}

      .price-box{display:flex;flex-direction:column;gap:0.4rem;}
      .price-item{display:flex;justify-content:space-between;border-bottom:1px solid rgba(255,255,255,0.08);padding-bottom:0.35rem;margin-bottom:0.35rem;font-size:0.9rem;}

      #walletAddress{color:var(--accent);margin-top:0.4rem;font-size:0.85rem;}
      footer{margin-top:2.5rem;font-size:0.75rem;color:var(--muted);border-top:1px solid rgba(30,64,175,0.3);padding-top:1rem;}
    </style>
  </head>

  <body>
    <div class="shell">
      <header>
        <div class="logo">
          <div class="logo-mark">A</div>
          <span>Amicbridge</span>
        </div>
        <nav>
          <a href="#invest">Invest</a>
          <a href="#how">How it works</a>
          <a href="#contact">Contact</a>
          <a class="button" href="#start">Get started</a>
        </nav>
      </header>

      <!-- HERO -->
      <section id="start" style="margin-bottom:3rem;">
        <h1 style="font-size:2.5rem;line-height:1.1;margin-bottom:1rem;">
          Borrow. Lend. <span style="color:var(--accent);">Invest in Crypto.</span>
        </h1>
        <p style="max-width:40rem;color:var(--muted);font-size:1rem;">
          Amicbridge lets you borrow money, lend to real people, or invest your crypto
          safely through smart-contract vaults and tokenized assets.
          You always keep control of your own wallet.
        </p>
        <div style="display:flex;gap:0.8rem;margin-top:1.2rem;">
          <a class="btn btn-primary" href="#invest">Invest in crypto</a>
          <a class="btn btn-outline" href="#borrow-form">Borrow crypto</a>
        </div>
      </section>

      <!-- WALLET CONNECT -->
      <section id="wallet" class="section-grid">
        <div class="card">
          <h2>Connect Wallet</h2>
          <p style="color:var(--muted);font-size:0.9rem;">
            Connect MetaMask or any Web3 wallet to access crypto investing,
            tokenized assets, and stablecoin transactions.
          </p>
          <button id="connectBtn" class="btn btn-primary" style="margin-top:1rem;">
            Connect Wallet
          </button>
          <div id="walletAddress"></div>
        </div>

        <!-- LIVE PRICES -->
        <div class="card">
          <h2>Live Asset Prices</h2>
          <div class="price-box">
            <div class="price-item"><span>Bitcoin (BTC)</span><span id="p-btc">--</span></div>
            <div class="price-item"><span>Ethereum (ETH)</span><span id="p-eth">--</span></div>
            <div class="price-item"><span>Gold (XAU)</span><span id="p-gold">--</span></div>
            <div class="price-item"><span>Silver (XAG)</span><span id="p-silver">--</span></div>
          </div>
          <p style="font-size:0.75rem;color:var(--muted);">
            Prices update automatically every 60 seconds.
          </p>
        </div>
      </section>

      <!-- INVEST SECTION -->
      <section id="invest" class="section-grid">
        <div class="card">
          <h2>Invest in Crypto Assets</h2>
          <p style="color:var(--muted);font-size:0.88rem;">
            Access curated crypto markets, stablecoins, tokenized commodities,
            and sharia-filtered investments. All investments run through
            secure smart contracts, and you remain in control of your wallet.
          </p>

          <p style="margin-top:1rem;">Supported assets:</p>
          <ul style="font-size:0.9rem;color:var(--muted);margin-left:1.2rem;">
            <li>Bitcoin (BTC)</li>
            <li>Ethereum (ETH)</li>
            <li>USDT / USDC Stablecoins</li>
            <li>DAI (decentralized stablecoin)</li>
            <li>Tokenized gold (XAU)</li>
            <li>Tokenized silver (XAG)</li>
          </ul>

          <a href="#vaults" class="btn btn-primary" style="margin-top:1.2rem;">
            View Investment Vaults
          </a>
        </div>

        <div class="card">
          <h2>Sharia-Filtered Investments</h2>
          <p style="color:var(--muted);font-size:0.88rem;">
            Investments shown on Amicbridge can be filtered according
            to general Islamic principles such as:
          </p>
          <ul style="font-size:0.9rem;color:var(--muted);margin-left:1.2rem;">
            <li>Asset-backed value</li>
            <li>No interest-based lending (Riba)</li>
            <li>Transparent cost and profit structure</li>
            <li>No gambling or speculative derivatives</li>
          </ul>
          <p style="margin-top:1rem;font-size:0.8rem;color:var(--muted);">
            Note: Sharia compliance requires qualified scholarly review.
            Amicbridge is exploring this framework as an optional filter,
            not a certified guarantee.
          </p>
        </div>
      </section>

      <!-- TOKENIZED ASSETS -->
      <section id="vaults" style="margin-top:3rem;">
        <h2 style="margin-bottom:1rem;">Tokenized Asset Marketplace</h2>

        <div class="token-grid">
          <div class="token-card">
            <strong>Tokenized Gold (XAU)</strong>
            <p style="color:var(--muted);font-size:0.8rem;">
              1:1 value backed by physical gold reserves.
            </p>
          </div>
          <div class="token-card">
            <strong>Tokenized Silver (XAG)</strong>
            <p style="color:var(--muted);font-size:0.8rem;">
              Silver exposure on-chain with real-asset backing.
            </p>
          </div>
          <div class="token-card">
            <strong>Real Estate Token Slots</strong>
            <p style="color:var(--muted);font-size:0.8rem;">
              Invest in fractional property-backed tokens.
            </p>
          </div>
          <div class="token-card">
            <strong>Sukuk-Style Bonds</strong>
            <p style="color:var(--muted);font-size:0.8rem;">
              Islamic-structured profit-sharing instruments.
            </p>
          </div>
        </div>
      </section>

      <!-- How it works -->
      <section id="how" class="section-grid">
        <div class="card">
          <h2>For borrowers</h2>
          <ul style="font-size:0.9rem;color:var(--muted);margin-left:1.2rem;">
            {featuresBorrower.map(item => <li>{item}</li>)}
          </ul>
        </div>
        <div class="card">
          <h2>For lenders</h2>
          <ul style="font-size:0.9rem;color:var(--muted);margin-left:1.2rem;">
            {featuresLender.map(item => <li>{item}</li>)}
          </ul>
        </div>
      </section>

      <!-- CONTACT -->
      <section id="contact" style="margin-top:3rem;">
        <div class="card">
          <h2>Contact</h2>
          <p><strong>CEO:</strong> Robert</p>
          <p><strong>Phone:</strong> 929 28 373</p>
          <p>
            <strong>Email:</strong>
            <a href="mailto:robertamicolsen@gmail.com" style="color:var(--accent);">
              robertamicolsen@gmail.com
            </a>
          </p>
        </div>
      </section>

      <footer style="margin-top:3rem;">
        <p>
          <strong>Important:</strong> Amicbridge does not hold user funds
          or guarantee investment returns. All crypto transactions are handled
          directly from user-owned wallets. Investments carry risk. Always do
          your own research.
        </p>
      </footer>
    </div>

    <!-- SCRIPT -->
    <script>
      // WALLET CONNECT
      const connectBtn = document.getElementById("connectBtn");
      const walletDisplay = document.getElementById("walletAddress");

      connectBtn.addEventListener("click", async () => {
        if (!window.ethereum) {
          alert("No Web3 wallet found. Install MetaMask.");
          return;
        }
        try {
          const accounts = await window.ethereum.request({
            method: "eth_requestAccounts"
          });
          walletDisplay.textContent = "Connected: " + accounts[0];
        } catch (err) {
          alert("Wallet connection cancelled.");
        }
      });

      // PRICE FETCH
      async function loadPrices() {
        try {
          const url =
            "https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,gold,silver&vs_currencies=usd";
          const res = await fetch(url);
          const data = await res.json();

          document.getElementById("p-btc").textContent = "$" + data.bitcoin.usd.toLocaleString();
          document.getElementById("p-eth").textContent = "$" + data.ethereum.usd.toLocaleString();
          document.getElementById("p-gold").textContent = "$" + data.gold.usd.toLocaleString();
          document.getElementById("p-silver").textContent = "$" + data.silver.usd.toLocaleString();
        } catch (err) {
          console.log("Price API error:", err);
        }
      }
      loadPrices();
      setInterval(loadPrices, 60000);
    </script>
  </body>
</html>
