---
const pageTitle = "Lindt Excellence – Spesialtilbud";
---

<html lang="no">
  <head>
    <meta charset="utf-8" />
    <title>{pageTitle}</title>
    <meta
      name="description"
      content="Bestill 3 eksklusive Lindt Excellence-sjokoladeplater for kun 150 NOK inkludert frakt."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preload" as="image" href="/images/lindt-excellence.webp">
    <style>
      :root {
        --bg-dark: #101014;
        --bg-light: #f5f5f7;
        --accent: #c9a35b;
        --accent-dark: #a88439;
        --text-main: #121212;
        --text-light: #ffffff;
        --border-soft: rgba(0, 0, 0, 0.06);
        --radius-xl: 20px;
        --shadow-soft: 0 18px 45px rgba(0, 0, 0, 0.16);
        --shadow-small: 0 8px 20px rgba(0, 0, 0, 0.1);
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        background: radial-gradient(circle at top, #22232a 0, #050509 55%);
        color: var(--text-main);
      }

      main {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        padding: 32px 16px 64px;
      }

      .page {
        width: 100%;
        max-width: 1120px;
        background: linear-gradient(180deg, #131419 0, #0a0b0f 45%, #050509 100%);
        border-radius: 32px;
        box-shadow: var(--shadow-soft);
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.04);
      }

      .page-inner {
        display: grid;
        grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
        gap: 32px;
        padding: 32px 32px 40px;
      }

      @media (max-width: 900px) {
        .page-inner {
          grid-template-columns: minmax(0, 1fr);
          padding: 24px 18px 32px;
        }
      }

      .branding {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 14px;
      }

      .brand-mark {
        width: 30px;
        height: 30px;
        border-radius: 999px;
        background: radial-gradient(circle at 30% 30%, #fef5dc, #b88b3e);
        border: 1px solid rgba(255, 255, 255, 0.6);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 16px;
        color: #3d2a08;
        box-shadow: var(--shadow-small);
      }

      .brand-name {
        text-transform: uppercase;
        letter-spacing: 0.18em;
        font-size: 11px;
        color: rgba(255, 255, 255, 0.7);
      }

      h1 {
        margin: 4px 0 12px;
        font-size: clamp(26px, 3vw, 32px);
        color: #ffffff;
      }

      .subtitle {
        font-size: 15px;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.78);
        max-width: 460px;
      }

      .price-line {
        margin: 18px 0 22px;
        display: flex;
        align-items: baseline;
        gap: 12px;
      }

      .price-main {
        font-size: 26px;
        font-weight: 650;
        color: #ffe9b7;
      }

      .price-detail {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.65);
      }

      .pill-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 22px;
      }

      .pill {
        padding: 4px 10px;
        border-radius: 999px;
        font-size: 11px;
        letter-spacing: 0.09em;
        text-transform: uppercase;
        border: 1px solid rgba(255, 255, 255, 0.16);
        color: rgba(255, 255, 255, 0.78);
        backdrop-filter: blur(10px);
      }

      .pill--gold {
        border-color: var(--accent);
        color: #f8e3b7;
        background: rgba(0, 0, 0, 0.45);
      }

      .cta-row {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin-bottom: 26px;
      }

      .btn-primary {
        border-radius: 999px;
        padding: 11px 18px;
        background: linear-gradient(135deg, #f5d99f, #c2903d);
        color: #201407;
        font-weight: 600;
        text-decoration: none;
        display: inline-block;
      }

      .products {
        margin-top: 10px;
        padding: 16px;
        border-radius: 18px;
        background: radial-gradient(circle at top left, #343848, #070810);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
      }

      .product-card {
        background: #121212;
        border-radius: 12px;
        padding: 10px;
        text-align: center;
      }

      .product-image img {
        width: 100%;
        border-radius: 8px;
        background: #fff;
      }

      .order-panel {
        background: var(--bg-light);
        border-radius: 28px;
        padding: 20px;
      }
    </style>
  </head>

  <body>
    <main>
      <section class="page">
        <div class="page-inner">

          <div>
            <div class="branding">
              <div class="brand-mark">R</div>
              <div class="brand-name">Eksklusivt tilbud</div>
            </div>

            <h1>Lindt Excellence gavepakke – 3 x 100 g</h1>
            <p class="subtitle">
              Tre nøye utvalgte premium-sjokoladeplater. Perfekt gave eller snacks.
            </p>

            <div class="price-line">
              <div class="price-main">150 NOK</div>
              <div class="price-detail">inkludert frakt i Norge</div>
            </div>

            <div class="pill-row">
              <div class="pill pill--gold">Trygg bestilling</div>
              <div class="pill">Betaling avtales på e-post</div>
            </div>

            <div class="cta-row">
              <a href="#bestill" class="btn-primary">Bestill pakken nå</a>
            </div>

            <div class="products">
              <div class="products-grid">
                <article class="product-card">
                  <div class="product-image">
                    <img src="/images/lindt-excellence.webp" alt="Lindt">
                  </div>
                  <div class="product-name">White Vanilla 100 g</div>
                </article>

                <article class="product-card">
                  <div class="product-image">
                    <img src="/images/lindt-excellence.webp" alt="Lindt">
                  </div>
                  <div class="product-name">Dark Sea Salt 100 g</div>
                </article>

                <article class="product-card">
                  <div class="product-image">
                    <img src="/images/lindt-excellence.webp" alt="Lindt">
                  </div>
                  <div class="product-name">Roasted Hazelnut 100 g</div>
                </article>
              </div>
            </div>
          </div>

          <div id="bestill" class="order-panel">
            <h2>Bestill Lindt Excellence-pakken</h2>

            <form id="order-form">
              <label>Fullt navn</label>
              <input name="name" required />

              <label>E-post</label>
              <input name="email" type="email" required />

              <label>Adresse</label>
              <input name="address" required />

              <label>Kommentar</label>
              <textarea name="message"></textarea>

              <button>Send forespørsel</button>
            </form>

            <p>CEO Robert – <a href="mailto:Robertamicolsen@gmail.com">Robertamicolsen@gmail.com</a></p>
          </div>
        </div>
      </section>
    </main>

    <script>
      const form=document.getElementById('order-form');
      form.addEventListener('submit',e=>{
        e.preventDefault();
        const d=new FormData(form);
        const body=encodeURIComponent(`Navn: ${d.get('name')}\nEpost: ${d.get('email')}\nAdresse: ${d.get('address')}\nKommentar: ${d.get('message')}`);
        window.location=`mailto:Robertamicolsen@gmail.com?subject=Lindt bestilling&body=${body}`;
      });
    </script>
  </body>
</html>
